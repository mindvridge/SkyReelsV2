# 🚀 macOS 환경에서 SkyReels V2 실행 가이드

## 📋 현재 환경에서 실행하기 위한 단계

### ✅ 1단계: 사전 준비 확인

#### Docker Desktop 설치 확인
```bash
docker --version
docker-compose --version
```

**이미 설치되어 있음**: ✅ Docker version 28.4.0, Docker Compose version v2.39.2

#### Docker Desktop 리소스 설정
1. Docker Desktop 실행
2. Preferences (⌘ + ,) → Resources
3. 다음 설정 확인:
   - **CPU**: 최소 4개 코어
   - **Memory**: 최소 8GB (권장 16GB)
   - **Disk**: 50GB+ 여유 공간

---

### 📁 2단계: 프로젝트 디렉토리로 이동

```bash
cd /Users/mindprep/Desktop/VideoMaker/SkyReelsV2/skyreels-app
```

---

### 🔍 3단계: 포트 사용 확인 (선택사항)

필요한 포트가 사용 중인지 확인:
- `8000`: 백엔드 API
- `3000`: 프론트엔드
- `5432`: PostgreSQL
- `6379`: Redis

```bash
# 포트 사용 확인
lsof -i :8000 -i :3000 -i :5432 -i :6379

# 사용 중이면 프로세스 종료
kill -9 <PID>
```

---

### 🐳 4단계: Docker Compose로 서비스 시작

#### 방법 A: 자동 실행 스크립트 사용 (권장) ⭐

```bash
cd /Users/mindprep/Desktop/VideoMaker/SkyReelsV2/skyreels-app
./start-mac.sh
```

스크립트가 자동으로:
- Docker 설치 확인
- 포트 사용 확인
- 서비스 시작
- 데이터베이스 초기화
- 상태 확인

#### 방법 B: 수동 실행

**옵션 1: Celery Worker 제외 (빠른 시작, 비디오 생성 불가) - 권장**

```bash
cd /Users/mindprep/Desktop/VideoMaker/SkyReelsV2/skyreels-app
docker-compose -f docker-compose.yml -f docker-compose.mac.yml up -d postgres redis backend frontend
```

**옵션 2: 전체 서비스 실행 (비디오 생성 가능, 매우 느림)**

```bash
cd /Users/mindprep/Desktop/VideoMaker/SkyReelsV2/skyreels-app
docker-compose -f docker-compose.yml -f docker-compose.mac.yml up -d
```

---

### 🗄️ 5단계: 데이터베이스 초기화

```bash
docker-compose -f docker-compose.yml -f docker-compose.mac.yml exec backend python -c "from app.models.database import init_db; init_db()"
```

**참고**: 이미 초기화된 경우 에러가 발생할 수 있지만 정상입니다.

---

### 📊 6단계: 서비스 상태 확인

```bash
# 실행 중인 컨테이너 확인
docker-compose -f docker-compose.yml -f docker-compose.mac.yml ps

# 로그 확인
docker-compose -f docker-compose.yml -f docker-compose.mac.yml logs -f

# 백엔드 health check
curl http://localhost:8000/api/v1/health
```

---

### 🌐 7단계: 애플리케이션 접속

브라우저에서 다음 URL로 접속:

- **프론트엔드**: http://localhost:3000
- **백엔드 API**: http://localhost:8000
- **API 문서 (Swagger)**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/api/v1/health
- **WebSocket 통계**: http://localhost:8000/ws/stats

---

## ⚠️ 중요 사항

### macOS 제약사항

1. **GPU 미지원**: macOS는 NVIDIA GPU를 지원하지 않습니다
2. **CPU만 사용**: 비디오 생성은 CPU로만 실행되며 매우 느립니다 (10-100배 느림)
3. **개발 목적**: 프론트엔드, 백엔드 API, 데이터베이스, WebSocket 기능 테스트는 정상 작동합니다
4. **실제 비디오 생성**: GPU가 있는 Linux 서버에서 실행하는 것을 권장합니다

### 정상 작동하는 기능

✅ 프론트엔드 UI (모든 컴포넌트)  
✅ 백엔드 API (모든 REST 엔드포인트)  
✅ 데이터베이스 (PostgreSQL CRUD)  
✅ WebSocket (실시간 통신)  
✅ 파일 업로드/다운로드  

### 제한적으로 작동하는 기능

⚠️ 비디오 생성 (CPU로만 실행, 매우 느림, 타임아웃 가능)

---

## 🔧 문제 해결

### 포트 충돌

```bash
# 포트 사용 확인
lsof -i :8000
lsof -i :3000

# 프로세스 종료
kill -9 <PID>
```

### 서비스 재시작

```bash
# 모든 서비스 재시작
docker-compose -f docker-compose.yml -f docker-compose.mac.yml restart

# 특정 서비스 재시작
docker-compose -f docker-compose.yml -f docker-compose.mac.yml restart backend
```

### 로그 확인

```bash
# 전체 로그
docker-compose -f docker-compose.yml -f docker-compose.mac.yml logs -f

# 특정 서비스 로그
docker-compose -f docker-compose.yml -f docker-compose.mac.yml logs -f backend
docker-compose -f docker-compose.yml -f docker-compose.mac.yml logs -f frontend
docker-compose -f docker-compose.yml -f docker-compose.mac.yml logs -f postgres
```

### 데이터베이스 재초기화

```bash
# 서비스 중지 및 볼륨 삭제
docker-compose -f docker-compose.yml -f docker-compose.mac.yml down -v

# 서비스 다시 시작
docker-compose -f docker-compose.yml -f docker-compose.mac.yml up -d postgres redis backend frontend

# 데이터베이스 초기화
docker-compose -f docker-compose.yml -f docker-compose.mac.yml exec backend python -c "from app.models.database import init_db; init_db()"
```

### 메모리 부족

Docker Desktop 설정에서 메모리 증가:
- Preferences → Resources → Memory를 16GB로 증가

---

## 🛑 서비스 중지

```bash
# 모든 서비스 중지
docker-compose -f docker-compose.yml -f docker-compose.mac.yml down

# 볼륨까지 삭제 (데이터 삭제)
docker-compose -f docker-compose.yml -f docker-compose.mac.yml down -v
```

---

## 📚 추가 문서

프로젝트 디렉토리에 다음 문서들이 있습니다:

- `QUICK_START.md`: 빠른 시작 가이드
- `SETUP_GUIDE.md`: 상세 설치 가이드
- `MACOS_SETUP.md`: macOS 설치 가이드
- `WEBSOCKET_SECURITY.md`: WebSocket 보안 문서
- `README.md`: 프로젝트 README

---

## 🎯 실행 요약

**가장 빠른 방법**:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /Users/mindprep/Desktop/VideoMaker/SkyReelsV2/skyreels-app

# 2. 자동 실행 스크립트 실행
./start-mac.sh

# 3. 브라우저에서 접속
# http://localhost:3000
```

**수동 실행 방법**:

```bash
# 1. 프로젝트 디렉토리로 이동
cd /Users/mindprep/Desktop/VideoMaker/SkyReelsV2/skyreels-app

# 2. 서비스 시작 (Celery Worker 제외)
docker-compose -f docker-compose.yml -f docker-compose.mac.yml up -d postgres redis backend frontend

# 3. 데이터베이스 초기화
docker-compose -f docker-compose.yml -f docker-compose.mac.yml exec backend python -c "from app.models.database import init_db; init_db()"

# 4. 브라우저에서 접속
# http://localhost:3000
```

---

**참고**: macOS에서는 개발/테스트 목적으로만 사용하고, 실제 비디오 생성은 GPU가 있는 Linux 서버에서 실행하는 것을 권장합니다.

